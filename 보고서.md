##  핵심 변수, 계산식 및 판단 로직

### 1. Camera1 — 책상캠 산만도 판정 (`cam1_ver5.py`)

주요 파라미터:
- `CONF_TH = 0.5` : YOLO 탐지 신뢰도 임계값
- `INFER_EVERY = 3` : 3프레임당 1회만 YOLO 추론(성능 최적화)
- `HISTORY_LEN = 15`, `PRESENT_TH = 5` : 최근 15프레임 중 5프레임 이상 감지되어야 "존재"로 확정 (깜빡임성 오탐 방지, 디바운싱)

판정 로직:
```
sum(distraction_hist[-15:]) >= 5  → factor = 0.4   (휴대폰 등 산만 요소 지속 감지)
elif sum(study_hist[-15:]) >= 5   → factor = 1.0   (책/노트북 지속 감지)
else                               → factor = 0.8   (기본값)
```
`factor`는 이후 `smoothed_score` 계산에 직접 곱해지는 **결과에 가장 큰 영향을 주는 변수 중 하나**이며, 산만 요소 발견 시 최종 점수를 강제로 최대 40%로 캡핑하는 효과를 가진다.

### 2. Camera2 — 얼굴캠 집중도 산출 (`cam2_ver5.py`)

주요 임계값:
- `EAR_BLINK_THRESHOLD = 0.1`, `BLINK_CONSEC_FRAMES = 2`, `BLINK_MIN_SEP = 0.35초` : 깜빡임 판정 및 디바운싱
- `EYE_VARIANCE_THRESHOLD = 200.0`, `EYE_MEAN_DARK = 45.0` : 눈 가림(occlusion) 판정
- `GAZE_X_DELTA = 0.09`, `GAZE_Y_DELTA = 0.09` : 캘리브레이션 기준 중심 허용 오차
- `NOISE_SENSITIVITY = 2.0` : baseline RMS의 2배 초과 시 소음
- `EYES_CLOSED_SECONDS = 3.0`, `NO_FACE_SECONDS = 10.0` : 연속 눈감음/얼굴미인식 지속시간 임계

**시선 방향 판정식**:
```
dx = avgx - baseline_x,  dy = avgy - baseline_y
|dx| ≤ 0.09 AND |dy| ≤ 0.09  → CENTER
|dx| > |dy|                   → LEFT (dx<0) / RIGHT (dx≥0)
else                           → UP (dy<0) / DOWN (dy≥0)
```

**고개 정렬 판정**: `head_ok = |nose_x-0.5| < 0.22 AND |nose_y-0.5| < 0.18`

**눈 가림(occlusion) 판정** (양쪽 눈 모두 해당해야 성립 — AND 조건):
```
(left_var<200 OR left_mean<45) AND (right_var<200 OR right_mean<45) → occluded = True
```

**집중도 점수 계산식** (`compute_concentration`, 이 프로젝트의 핵심 판단식):
```
gaze_score = 1.0 if gaze_ok else 0.0
head_score = 1.0 if head_ok else 0.0
blink_pen  = 0.5 if blink_recent else 0.0
noise_pen  = 1.0 if noise_flag else 0.0

base = 0.4·gaze_score + 0.3·head_score + 0.2·(1-blink_pen) + 0.1·(1-noise_pen)
if occluded: base *= 0.2        # 가림 발생 시 강력 페널티(80% 감점)
score = clip(base × 100, 0, 100)
```
가중치는 **시선 40% > 고개 30% > 깜빡임 20% > 소음 10%** 순으로 설계되어 시선이 가장 중요한 판단 근거임을 보여준다. `occluded`는 다른 모든 점수를 무력화하는 최우선 조건(곱셈 페널티)이다.

**점수 비선형 보정** (사용자 체감 점수를 상향하기 위한 개발자 자체 설계):
```
smooth_score = int((1 - ((100 - smooth_score)/100)²) × 100)
```
예: 50→75, 70→91, 80→96. 원점수가 낮을수록 보정폭이 커지는 오목(concave) 곡선으로, "너무 낮게 나오는" 원점수를 체감상 완화한다.

**상태(status) 우선순위 판정** (여러 조건의 결합 → 최종 라벨):
```
NO FACE  (얼굴/신뢰도 미달)
  > OCCLUDED (가림)
    > NOISY (소음, baseline×2 초과)
      > BLINK (깜빡임 이벤트)
        > DISTRACTED (score < 55)
          > CONCENTRATED (그 외)
```

**얼굴 미인식 자동 종료**: `no_face_elapsed ≥ NO_FACE_SECONDS(10초) → should_exit = True` → 메인 루프가 즉시 종합 평가 화면으로 전환.

### 3. 점수 병합 및 최종 지표 (`main_ver5.py` / `main.py`)

```
raw_score = factor(Camera1) × score(Camera2)
smoothed_score = ALPHA·raw_score + (1-ALPHA)·smoothed_score_prev   (ALPHA = 0.5)
```
`factor ∈ {0.4, 0.8, 1.0}`이 곱해지므로, 책상캠에서 휴대폰이 지속 감지되면 얼굴캠 점수가 만점이어도 최종 점수는 40점을 넘지 못한다 — **두 카메라 중 하나라도 "산만"으로 판정하면 전체 점수가 급락하는 구조**.

### 4. 종합 평가 등급 (`ui_ver5.py: classify_focus_level`)
```
avg_score < 20   → 매우 낮음
avg_score < 40   → 낮음
avg_score < 60   → 보통
avg_score < 80   → 높음
그 외(≥80)        → 매우 높음
```
`GRAPH_SAMPLE_SEC`(데스크톱 5초 / 웹 10초) 주기로 `sample_buffer` 평균을 `graph_points`와 `subject_totals`에 누적하며, 일시정지/종료 시점에 남은 표본은 `flush_leftover_sample()`로 강제 반영해 데이터 유실을 방지한다.
